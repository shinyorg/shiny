<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:shiny="http://shiny.net/maui/controls"
             xmlns:vm="clr-namespace:Sample.Shared.Maui.Pages.Locations"
             x:Class="Sample.Shared.Maui.Pages.Locations.GpsPage"
             x:DataType="vm:GpsViewModel"
             Title="GPS">
    <shiny:TableView>
        <shiny:TableRoot>
            <shiny:TableSection Title="Configuration">
                <shiny:TextPickerCell Title="Background Mode"
                                   ItemsSource="{Binding BackgroundModes}"
                                   SelectedIndex="{Binding SelectedModeIndex, Mode=TwoWay}" />
                <shiny:SwitchCell Title="Precise Accuracy" On="{Binding RequestPreciseAccuracy, Mode=TwoWay}" />
            </shiny:TableSection>

            <shiny:TableSection Title="iOS Options" IsVisible="{Binding IsIos}">
                <shiny:TextPickerCell Title="Activity Type"
                                   ItemsSource="{Binding ActivityTypes}"
                                   SelectedIndex="{Binding SelectedActivityTypeIndex, Mode=TwoWay}" />
                <shiny:SwitchCell Title="Background Indicator" On="{Binding ShowsBackgroundLocationIndicator, Mode=TwoWay}" />
                <shiny:SwitchCell Title="Auto Pause Updates" On="{Binding PausesLocationUpdatesAutomatically, Mode=TwoWay}" />
            </shiny:TableSection>

            <shiny:TableSection Title="Android Options" IsVisible="{Binding IsAndroid}">
                <shiny:TextPickerCell Title="GPS Priority"
                                   ItemsSource="{Binding GpsPriorities}"
                                   SelectedIndex="{Binding SelectedGpsPriorityIndex, Mode=TwoWay}" />
                <shiny:SwitchCell Title="Wait for Accurate Location" On="{Binding WaitForAccurateLocation, Mode=TwoWay}" />
                <shiny:SwitchCell Title="Stop Service With Task" On="{Binding StopForegroundServiceWithTask, Mode=TwoWay}" />
            </shiny:TableSection>

            <shiny:TableSection Title="Reading Filters (Delegate)">
                <shiny:EntryCell Title="Min Distance (m)" ValueText="{Binding MinDistanceMeters, Mode=TwoWay}" Keyboard="Numeric" Placeholder="blank = off" />
                <shiny:EntryCell Title="Min Time (sec)" ValueText="{Binding MinTimeSeconds, Mode=TwoWay}" Keyboard="Numeric" Placeholder="blank = off" />
                <shiny:EntryCell Title="Max Distance (m)" ValueText="{Binding MaxDistanceMeters, Mode=TwoWay}" Keyboard="Numeric" Placeholder="blank = off" />
                <shiny:EntryCell Title="Max Time (sec)" ValueText="{Binding MaxTimeSeconds, Mode=TwoWay}" Keyboard="Numeric" Placeholder="blank = off" />
                <shiny:LabelCell Title="Filter Status" ValueText="{Binding FilterStatus}" />
            </shiny:TableSection>

            <shiny:TableSection Title="Actions">
                <shiny:ButtonCell Title="{Binding ListenText}" Command="{Binding ToggleListenerCommand}" />
                <shiny:ButtonCell Title="Get Current Position" Command="{Binding GetCurrentPositionCommand}" />
                <shiny:ButtonCell Title="Check Permission" Command="{Binding CheckPermissionCommand}" />
                <shiny:LabelCell Title="Status" ValueText="{Binding Status}" />
            </shiny:TableSection>

            <shiny:TableSection Title="Current Reading">
                <shiny:LabelCell Title="Latitude" ValueText="{Binding Latitude, StringFormat='{0:F6}'}" />
                <shiny:LabelCell Title="Longitude" ValueText="{Binding Longitude, StringFormat='{0:F6}'}" />
                <shiny:LabelCell Title="Altitude" ValueText="{Binding Altitude, StringFormat='{0:F1}m'}" />
                <shiny:LabelCell Title="Speed" ValueText="{Binding Speed, StringFormat='{0:F1} m/s'}" />
                <shiny:LabelCell Title="Heading" ValueText="{Binding Heading, StringFormat='{0:F1}\u00B0'}" />
                <shiny:LabelCell Title="Position Accuracy" ValueText="{Binding PositionAccuracy, StringFormat='{0:F1}m'}" />
                <shiny:LabelCell Title="Heading Accuracy" ValueText="{Binding HeadingAccuracy, StringFormat='{0:F1}\u00B0'}" />
                <shiny:LabelCell Title="Speed Accuracy" ValueText="{Binding SpeedAccuracy, StringFormat='{0:F1} m/s'}" />
                <shiny:LabelCell Title="Timestamp" ValueText="{Binding Timestamp}" />
                <shiny:LabelCell Title="Floor" ValueText="{Binding Floor}" />
                <shiny:LabelCell Title="Stationary" ValueText="{Binding IsStationary}" />
            </shiny:TableSection>
        </shiny:TableRoot>
    </shiny:TableView>
</ContentPage>
