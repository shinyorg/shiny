<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Samples.Notifications.SetupPage"
             Title="Setup">
    <ContentPage.Content>
        <TableView Intent="Menu">
            <TableRoot>
                <TableSection>
                    <TextCell Text="Request Permissions (iOS only)" Command="{Binding PermissionCheck}" />
                    <TextCell Text="Send Test" Command="{Binding SendTest}" />
                </TableSection>
            </TableRoot>
            <!--
            <Label Text="Badge" />
            <Entry Text="{Binding Badge}"
                   Keyboard="Numeric"
                   MaxLength="2" />
            <Button Text="Vibrate" Command="{Binding Vibrate}" />-->
        </TableView>
    </ContentPage.Content>
</ContentPage>